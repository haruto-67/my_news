# 自分専用ニュース配信サービス 要件定義

## 1. 概要 / 位置づけ

自分専用のニュース配信サービス。毎朝、興味に沿った情報を自動で収集し、Claude Code がキュレーション・要約・分類したうえで配信する。

役割分担は以下の2系統:

- **Discord（通知）**: 新着ダイジェストを毎朝プッシュ通知。ざっと眺める速報用。
- **GitHub Pages（母艦ダッシュボード）**: 一覧・アーカイブ・カテゴリ絞り込み・お気に入りを備えたメイン画面。振り返り・検索用。

## 2. 目的

- **主目的**: 自分の運用環境（にわとりMC / PaperMC / Java / Rust / Raspberry Pi 等）に影響しそうな脆弱性・重要アップデートを見逃さない。
- **副目的**: 趣味（Minecraft全般）と広めのITニュースを毎朝把握できる。
- **サービスとしての性質**: 「こういう記事もほしい」を後から簡単に足せること。設定ファイルの編集だけで購読内容・興味の方向を調整できる（UI不要でも成立する構成）。

---

## 3. カテゴリ / 出力の構成

配信・表示は複数カテゴリで構成する。カテゴリは設定ファイルで定義され、追加・変更が容易であること。初期カテゴリは以下の3つ:

### ① 趣味
- Minecraft 全般（技術に限らない）: 最新アップデート・スナップショット・リリース、データパック、RTA/速攻攻略の話題や記録更新
- その他ゲーム・趣味の話題

### ② 自分に関連するIT
- 自分が実際に触る技術スタックに関わるもの: PaperMC / Minecraftサーバー運用、Java / JDK、Rust、Raspberry Pi / Linux / systemd、MySQL、nginx など
- **脆弱性情報を最優先**。「このカテゴリだけ見ておけば、自分の環境で急いで対応すべき脆弱性があるか判断できる」状態を目指す
- 該当する CVE / JVN があれば深刻度と対象バージョンを明記

### ③ もっと広いIT
- AI業界の動向、開発者が知っておくと良い時事ネタ（例：Claude共有リンクがGoogle検索にインデックスされ話題化、のような）

---

## 4. 情報ソース方式（ハイブリッド）

取りこぼし厳禁のものは固定ソース（RSS）で確実に拾い、発見性・視野の広さが欲しいものは Claude のネット探索で補う。両者を組み合わせる。

- **固定ソース（feeds）**: RSS/API で確実に取得。特に脆弱性系は必ずここに含める（運任せにしない）。
- **探索（explore）**: カテゴリの説明文（description）を手がかりに、Claude Code がネット探索（WebSearch）して関連記事を発見する。特定リンクへの依存による偏りを避ける目的。
- 探索の有無は**カテゴリごとに切り替え可能**（例：脆弱性カテゴリは探索オフで固定ソースのみ、という運用も可）。

---

## 5. 設定ファイル（feeds.yml）

購読設定・興味プロファイルはコードから分離し、1つの設定ファイルに外出しする。GitHub上でこのファイルを編集すること＝設定変更、となる。

構造イメージ:

```yaml
categories:
  - name: 趣味
    description: |
      Minecraft全般。最新アップデート、スナップショット、
      データパック、RTA/速攻攻略の記録更新など。技術寄りでなくてOK。
    feeds:                    # 確実に拾う固定ソース（複数指定可、空でも可）
      - https://minecraft.net/...
      - https://papermc.io/...
    explore: true             # descriptionを手がかりにネット探索も行う

  - name: 自分に関連するIT
    description: |
      自分が触る技術スタックの脆弱性・重要アップデートを最優先。
      PaperMC / Java / JDK / Rust / Raspberry Pi / Linux / systemd /
      MySQL / nginx。CVE/JVN該当時は深刻度と対象バージョンを明記。
    feeds:
      - https://jvndb.jvn.jp/ja/feed/   # 脆弱性は取りこぼし厳禁
    explore: true

  - name: もっと広いIT
    description: |
      AI業界の動向、開発者が知っておくと良い時事ネタ。
    feeds: []                 # 固定ソースなし、探索のみ
    explore: true
```

### 設計上の要点
- **feeds は各カテゴリに複数指定可能**（リスト構造。何本でも追加でき、空 `[]` も可）。
- **description が二役を兼ねる**: ①収集した記事を分類する際の基準、②explore 探索時に「何を探すべきか」を Claude Code に伝える指示。1箇所を編集すれば分類と探索の両方が同時に調整される。
- **拡張運用**: 「こういう記事もほしい」→ description に一文追加。「この固定ソースも確実に見たい」→ feeds に一行追加。「カテゴリを増やしたい」→ ブロックをコピー。いずれもファイル編集のみで完結。
- 将来的に UI を被せる場合も、このファイルを生成・編集するフォームを載せるだけで拡張できる。

---

## 6. アーキテクチャ / 処理フロー

実行の主体は **本番サーバー（Raspberry Pi / Ubuntu）**。Claude Code CLI をローカルで非対話実行して要約を生成するため、実行基盤はクラウドではなくサーバー側に置く。GitHub Actions は「push を受けて Pages を更新するだけ」に専念する。

```
[ Raspberry Pi / Ubuntu ]  cron 毎朝 5:00 起動
  ├─ feeds.yml を読む（購読設定・興味プロファイル）
  ├─ collect  固定ソース(RSS/API)を収集 + explore対象はネット探索
  ├─ curate   claude -p（非対話モード）にパイプで投げて分類・要約
  │             例: cat articles.json | claude -p "分類・要約して。基準は…" --output-format json
  ├─ data/YYYY-MM-DD.json を生成
  ├─ ① GitHub へ push
  └─ ② Discord webhook へ通知（要約が手元にある Pi 側で直接叩く）

[ GitHub Actions ]  push を契機に起動
  └─ Pages をビルド & デプロイ（それだけ）

[ GitHub Pages ]
  └─ data/*.json を読み込んでダッシュボード表示
```

### 責務分担の意図
- Claude Code はサブスク認証で動く対話型CLI。クリーンなCI環境で毎日ヘッドレス認証を通すのは非現実的なため、認証済みの Pi で実行する。
- 要約済み JSON は Pi の手元にあるので、Discord 通知も Pi 側で完結させる（Actions が JSON を読み直す二度手間・secret の二重管理を避ける）。
- Actions は Pages デプロイのみに限定し、責務を明確に分離する。

### キュレーションの方針（Claude Code への指示）
- 各カテゴリの description を「そのカテゴリの人格」として渡す。
- 興味に合わないものは除外してよい（全件羅列しない）。
- ②では自分の環境に該当しうる脆弱性を優先的に拾い、緊急度が分かる形で要約（深刻度・対象バージョン明記）。
- 各記事は「タイトル + 1〜2行要約 + 元リンク」を基本フォーマットに。

---

## 7. 出力先の仕様

### Discord（通知 / Pi 側から送信）
- **専用の1人サーバー**を作成し、そこへ **webhook** で投稿する（DMではなくチャンネル。1人サーバーで実質DM相当の体験）。
- 送信元は Pi のスクリプト（要約生成直後に webhook へ POST）。
- 既存の JDA bot（にわとりMC専用）とは完全に独立。常駐プロセス・gateway接続は不要で、HTTP POST のみ。
- カテゴリごとにチャンネルを分けても良い（趣味 / 脆弱性・自分関連 / 広いIT など）。
- フォーマット: カテゴリごとに embed を分け、記事ごとにタイトル・要約・リンクを表示。

### GitHub Pages（母艦ダッシュボード）
- **静的サイト**（サーバー不要）。`data/*.json` を読み込んで表示。
- 機能:
  - 日次ダイジェストの一覧表示
  - 日付アーカイブ（過去分の振り返り）
  - カテゴリによる絞り込み
  - **お気に入り（後で読む）**: ブラウザの localStorage に保存（サーバー不要）
- push を契機に GitHub Actions がビルド & デプロイ。
- 実装スタックはおまかせ（後述の制約の範囲で実装側が選定）。

---

## 8. 実行環境 / スケジュール

- **本番環境**: Raspberry Pi 上の Ubuntu。cron でスクリプトを起動。
- **開発環境**: macOS。→ cron/systemd・パス・シェル挙動・claude CLI の認証場所などが Mac と Ubuntu で異なるため、OS差異を意識した実装とすること（本番デプロイ先は Ubuntu）。
- **起動**: 毎朝 5:00 に cron で実行。
- **収集範囲**: 直近24時間の新着。
- **秘密情報**: Discord webhook URL は Pi 側で安全に管理（環境変数 / .env 等、リポジトリに含めない）。GitHub への push 認証も同様。

---

## 9. 技術的制約（要件レベル）

> 使用言語・ライブラリ・フレームワークは指定しない（実装側＝Claude Code が要件に沿って選定する）。以下は要件から導かれる制約のみ。

- 生成処理の実行基盤は本番サーバー（Pi / Ubuntu）。cron 起動でヘッドレスに完結すること。
- 要約生成は Claude Code CLI の非対話モード（`claude -p`）を利用（API課金を避ける）。標準入出力でリクエスト/レスポンスをやりとりし、HTTPレイヤは挟まない。
- ネット探索は Claude Code の WebSearch を利用（explore カテゴリ）。非対話実行時に探索が動くよう権限設定を行うこと（実装時に検証）。
- 出力インターフェースは「data/*.json の生成 → GitHub へ push」＋「Discord webhook への POST」。
- GitHub Actions は Pages のビルド & デプロイのみを担う。
- 設定は feeds.yml に外出しし、コードと分離すること。
- Pages は静的サイトとして成立すること。お気に入りは localStorage で保持。

---

## 10. 未確定事項（実装フェーズで決める）

- 各カテゴリの初期 feeds（実 URL）と description の最終文面。
- explore 探索の実装方法・1回あたりの探索範囲、非対話実行時の WebSearch 権限設定の検証。
- 新着判定のロジック（同一記事の重複配信をどう防ぐか。過去push分との差分管理）。
- 該当0件カテゴリの扱い（省略 or「なし」表示）。
- 各カテゴリの記事上限（最大 N 件など）。
- Discord のチャンネル構成（カテゴリ別に分けるか単一か）。
- cron 実行失敗時のリトライ / 通知（生成やpushが失敗した際の扱い）。
- claude -p の出力フォーマット安定化（JSON崩れ対策）。
